#include "events_h"
#include "utility_h"


void main ()
{

// load and run the cutscene
CS_LoadCutscene(R"ir_em_b.cut");
//--replace this with the name of your cutscene (include the .cut extension). DO NOT touch the R. that belongs there like that.
PlayCutscene();
//--then this plays it



}